#include <bits/stdc++.h>
using namespace std;

pair<int, int> with_array()
{
    vector<pair<int, int>> a;
    for (int i = 0; i < 3; i++)
    {
        int x, y;
        cin >> x >> y;
        a[i].first = x, a[i].second = y;
    }
}

int main()
{

    return 0;
}
