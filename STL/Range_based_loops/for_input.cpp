#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
int main()
{

    ll n;
    cin >> n;

    vector<ll> x(n), y(n);

    for (auto &it : x)
        cin >> it;

    for (auto &it : y)
        cin >> it;
}