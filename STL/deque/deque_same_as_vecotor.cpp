#include <bits/stdc++.h>
using namespace std;

int Deque()
{
    // everything is same as vectors and list
    deque<int> a = {234, 443, 23, 435, 423, 32};
    a.push_back(2);
    a.push_front(89);
    a.pop_back();
    a.pop_front();

    // everything is same
}
int main()
{
    Deque();
    return 0;
}